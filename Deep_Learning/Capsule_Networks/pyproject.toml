[project]
name = "capsule-tutorial"
version = "0.1.0"
description = "Capsule Networks Tutorial"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["torch>=2.0.0"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
