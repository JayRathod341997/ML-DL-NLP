[project]
name = "unet-tutorial"
version = "0.1.0"
description = "U-Net Tutorial with PyTorch"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["torch>=2.0.0", "torchvision>=0.15.0"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
