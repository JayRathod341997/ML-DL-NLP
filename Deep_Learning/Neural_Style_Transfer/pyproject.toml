[project]
name = "nst-tutorial"
version = "0.1.0"
description = "Neural Style Transfer Tutorial"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["torch>=2.0.0", "torchvision>=0.15.0"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
