"""
Exercise: Longer sequences

Task:
1) Increase sequence length to 20 (generate in-memory).
2) Change label rule to: y=1 if there exists a run of 5 consecutive 1s.
3) Compare RNN vs GRU vs LSTM training accuracy.
"""

raise SystemExit(
    "Exercise file: implement longer sequence rule + compare RNN/GRU/LSTM. See `04_solution.py` after you try."
)

